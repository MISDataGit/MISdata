% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda.R
\name{get_pacf}
\alias{get_pacf}
\title{Partial Autocorrelation Function (PACF)}
\usage{
get_pacf(data, symbols = NULL, column = "Close", lag_max = 40, plot = FALSE)
}
\arguments{
\item{data}{Long data.frame (Date/Symbol/Column/Value) or wide xts.}

\item{symbols}{character vector. One or more symbols (max 5). Required.}

\item{column}{character. Series from which simple returns are calculated.
Default: "Close".}

\item{lag_max}{integer. Maximum lag. Default: 40.}

\item{plot}{logical. If TRUE, returns a list with both the table and a
faceted ggplot. Default: FALSE (table only).}
}
\value{
\code{plot = FALSE}: data.frame with columns \code{lag, pacf, symbol}.
\code{plot = TRUE} : \code{list(table = data.frame, plot = ggplot)}.
}
\description{
Calculates simple returns from the selected series and computes PACF values
for one or more symbols. Results are returned as a long data.frame, with an
optional faceted bar-chart visualization. Returns are calculated as
\eqn{r_t = (x_t / x_{t-1}) - 1}.
}
\examples{
\dontrun{
tbl <- get_pacf(MIS_data, symbols = c("AAPL","MSFT"), lag_max = 30)
res <- get_pacf(MIS_data, symbols = "AAPL", plot = TRUE)
res$plot
}

}
